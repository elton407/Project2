var express = require("express");
var router = express.Router();
//REMEMBER TO EDIT DB NAME
var db = require("../models/");

//logic for generating random PIN
// function joinEvent (
//     if (host = TRUE){
//         var PIN = Math.floor(100000 + Math.random() * 900000);
//         //post route to create in sql
//         router.post("/event/create", function(req, res) {
//             db.Event.create({
//             PINvitation: req.body.PINvitation
//             })
//             //pass the result of our call
//             .then(function(dbEvent) {
//             console.log(dbEvent);
//             res.redirect("/event/" + PIN);
//             });
//         });
//     }
//     else {
//         //note: we will need to define the guestPIN as user input on the front page
//         res.redirect("/event/"+ PIN);
//     };
// );

// //Need to add aspects that allow user input like a jquery button click for the events or join as a guest
// joinEvent();


//////////////////4 reelz///////////////////////////////

if (host) {
    
} else {
    
}